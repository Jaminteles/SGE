# Base da API REST (NestJS). Em desenvolvimento o padrão "/api/v1" usa o proxy
# do Vite (ver vite.config.ts) e evita CORS.
VITE_API_URL="/api/v1"

# Origem real da API usada pelo proxy do Vite em desenvolvimento.
VITE_DEV_API_PROXY="http://localhost:3000"
